class Contact {
  String nom;
  String prenom;
  int age;
  String email;
  String photo;
  bool selContact; // Add this property

  Contact(this.nom, this.prenom, this.age, this.email, this.photo, this.selContact);
}
